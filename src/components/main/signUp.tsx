import React from "react";
import { useRecoilValue } from "recoil";

function signUp({ userType }: { userType: "관리자" | "회원" }): JSX.Element {
  //   const step = useRecoilValue(stepNum);

  return <>{/* {step >= 3 && <} */}</>;
}
